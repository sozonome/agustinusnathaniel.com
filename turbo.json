{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"outputs": [".svelte-kit/**", ".vercel/**", "public/**"],
			"env": ["VITE_NOTION_TOKEN", "VITE_BLOG_DATABASE_ID", "VITE_UMAMI_SRC"]
		},
		"lint": {
			"outputs": [],
			"inputs": [".eslintrc.*", "src/**"]
		}
	}
}
